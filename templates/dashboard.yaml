---
apiVersion: kubeaddons.mesosphere.io/v1alpha1
kind: Addon
metadata:
  name: dashboard
  namespace: kubeaddons
  labels:
    kubeaddons.mesosphere.io/name: dashboard
  annotations:
    appversion.kubeaddons.mesosphere.io: "1.10.1"
    endpoint.kubeaddons.mesosphere.io/dashboard: "/ops/portal/kubernetes/"
spec:
  kubernetes:
    minSupportedVersion: v1.14.0
  namespace: kube-system
  chartReference:
    chart: stable/kubernetes-dashboard
    version: 1.5.2
    values: |
      ---
      # so that kubectl proxy works
      fullnameOverride: 'kubernetes-dashboard'
      enableInsecureLogin: true
      service:
        externalPort: 9090
      resources:
        limits:
          cpu: 1000m
          memory: 1000Mi
        requests:
          cpu: 50m
          memory: 30Mi
